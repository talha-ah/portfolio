export interface SkillType {
  list: string
  title: string
}

export interface ProjectType {
  title: string
  link: string
  skills: string
  slug: string
  role: string
  description: string
  highlights: string[]
  shortDescription: string
}
